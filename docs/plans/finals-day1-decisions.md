# 本番初日の決定事項リスト+これまでの重要決定の振り返り

> 目的: 本番(ハッカソン本選)初日のシミュレーションの「総合的な感じ」から決めると
> ユーザーが宣言した事項を、**判断材料・選択肢・既定(縮退)値つき**で一覧化する(2026-07-20 第39バッチ)。
> 後半は過去の重要決定の振り返り表。実測手順の詳細は [finals-hardware-plan.md](./finals-hardware-plan.md) §2(Day-0ベンチ)。

---

## §1 本番初日(Day-0/Day-1)に決める事項

> **既定値 = 迷ったら/時間切れならこれで回す**という縮退線。決定は devlog に必ず記録する。

| # | 決定事項 | 選択肢 | 判断材料(Day-0/初日に実測・観察するもの) | 既定(縮退) |
|---|---|---|---|---|
| D1 | **視覚方式の最終選択**(ユーザー: F実装済み・当面B作動・初日に総合判断) | B のみ / F(B+v1 ON) / v1の発火率調整 | VLM 実 req/s(A5000実測)・レンダ ms/画像・v1発火率・テキストGPU 7→6枚の容量−14%許容・v0だけで発話の具体性が足りるか(agent-vision.md §5) | **B のみ作動**(v1 OFF) |
| D2 | **S7方針キャッシュの採否**(実装済み・既定OFF運用) | OFF / ON(reuse_rate_cap調整) | ブラインドA/B(scripts/compare_policy_cache.py)=行動多様性・k*信号への影響・LLM呼削減率(mock実測22.2%) | **OFF** |
| D3 | **主力テキストモデル**(小型モデルA/B) | 4B / 8B INT4 / 混成(purpose別Router) | Day-0実測 req/s(4B ~38 vs 8B ~21 prefix込)・日本語品質・belief書き戻し率・fallback率 | 4B(実測が仕様書前提) |
| D4 | **規模N(present_cap)とシミュ日数** | 在場5万〜25万 × 3〜10日 | Day-0ベンチ: 実効req/s と非LLM単価c(実測0.000528@1万深夜帯)→ 24h重畳での消化量逆算(人数>時間の優先はユーザー決定済み) | ベンチ結果から算出表で当日決定 |
| D5 | **LLM一括発行(S6b)のworkers数** | 1 / 4 / 8 / 16 | vLLM側の継続バッチング飽和度・タイムアウト率(ON=OFFバイト一致は担保済み) | ON・workers 8 |
| D6 | **GPU役割分割** | テキスト7+VLM0 / 6+1 / 5+2 | D1と連動。VLM実req/sとv1発火数の需給 | 7+0(BのみならVLM不要) |
| D7 | **ペルソナLLM上塗り率** | 0% / 2% / 5%(llm_targets.json) | 上塗り所要時間(本選前夜に前倒し可)・観察対象層(役割者・議員)の質 | 2%(役割層優先) |
| D8 | **保存ポリシー最終確認**(承認済み: 前景+サンプル全文) | サンプル率・画像ストア上限 | ストレージ実容量・イベント量実測(試算4.68億/10日・streaming集計は786MB実証) | 承認済み設定のまま |
| D9 | **縮退線の発動基準** | density 0.15→0.10→0.07 / present_cap段階縮小 / v0注入行数削減 | fallback率>1%・LLM待ち行列長・壁時計遅延(チェックポイント時刻で判定) | 段階縮小の順序表どおり |
| D10 | **行間レイヤ較正値の微調整** | C2会話密度(既定30/人日)・S4寄り道0.153・S5退屈閾値 | 初日ログの kpi(会話/人日・寄り道率・探索/人日)が現実レンジ(interstitial-life.md 較正表)に入っているか | 既定値のまま |
| D11 | **SUMOライブ連成v1のgo/no-go** | オフライン(v0済) / ライブ連成 | CPU余力(SUMO 1コア常駐)・背景交通の見た目要件 | オフラインのまま |
| D12 | **k掃引ランの開始タイミング**(本番=観察ラン承認済み) | 初日から並行 / 観察ラン安定後 | 初日の安定性(エラー率・checkpoint成功)・GPU余力 | 観察ラン1日完走後に開始 |
| D13 | **火種介入(spark treatment)条件を回すか**(第53バッチ・ON/OFF比較にGPU予算を割くか) | 回さない(介入ゼロで純観察のみ) / spark ON対照を1本追加(3メニュー全部) / メニュー個別ON(関係のみ/資本のみ/アンカーのみ)で寄与を分解 | 「立ち上げには初期介入が不可欠」という助言 vs「介入ゼロで観測」方針の対立を実験で決着=**立ち上がり速度**(sparked/非sparkedの活動量比・transmission波及hop数=trace_spark.py)と**持続性**(初期の差が時間で消えるか残るか=spark_reach/money_ratio列とdecay後の追跡)の比較。介入は初期条件・環境の差のみ(台本注入なし)・選抜は**trait-blind**(pid×spark.seedの純関数=生得/創発の交絡を排除=k*研究の清潔さを保つ)。GPU予算は観察ラン+k掃引(D12)が優先=sparkは**余剰があれば**の追加対照。 | **OFF=spark系条件は既定で回さない**(日常ベースライン優先。純観察を主・sparkは余剰枠での追加対照)。方針正典: closed-world-daily-observation.md §2.1「日常から何が生まれるかを観る」=強制トリガーは指紋になり日常創発との区別を潰すため既定から外す。 |
| D14 | **本番ランのモード選択(再現性実験 vs 純観察)**(第54バッチ・ユーザー要望2026-07-23: 不確実性許容モードの選択) | 再現性実験モード(数値seed固定・対照可能) / 純観察モード(`conf/observe.yaml`・`run.seed=auto`採取記録・chance偶発層[臨時収入/財布紛失/偶然の出会い]+既存揺らぎ[確率的実行/退屈好奇心/天候/関係系]をON=不確実性を許容) | **対照実験(k掃引=D12・spark ON/OFF=D13)の予定有無**が判断材料: 予定があるなら再現性実験モードで固定seed(同条件反復が要る)。純観察のみなら不確実性を許容し「エージェント視点の不確実な世界」を厚くして観る。**決定論エンジン自体は両モードで不変**(同seed同結果)=純観察ランも採取seed(config.yaml/summary.jsonに記録)で事後に1バイト違わず完全再現できる=「選ばないだけで失わない」。chance の daily_rate/money幅を上げると income の運感度が上がる(audit_uncertainty/analyze_luck で観測=第51が設計した運/実力分解に自動接続)。 | **再現性実験モード**(固定seed。対照実験=k*研究を主とするため。純観察はobserve.yaml+seed=autoで余剰枠) |
| D15 | **長期日常ラン1条件(30日級)を本選で確保するか**(第57バッチ・タスクC 2026-07-24: 構造創発の観察に必要な時間スケール) | 確保する(`conf/longrun30.yaml`=30日×絞った人数[既定100体]・checkpoint分割・観測レンズ全系ON で「強制トリガー無しで社会構造は変化/固着するのか」を1条件観る) / 確保しない(k*=7–14日ランに短縮=縮退) / 中間(14日で妥協) | **二段構え**(k*測定=7–14日・人数多め / 構造創発=1条件のみ30日以上・人数絞る)の後段を回すか。判断材料 ①**GPU予算**: 観察ラン+k掃引(D12/D4)が優先=30日は余剰枠。②**30日外挿値**(`scripts/bench_longrun.py`→`docs/research/longrun-estimate.md` の実時間/L1 parquetサイズ/ピークRAM。mockエンジン単価=実LLMでは req/s 律速へ変わる)。③**構造固着が7–14日で既に見えるか**(`scripts/analyze_structure.py` の固着検知[中心性turnover/edge churn/順位τ]・コミュニティ変化が短期で有意差を出すなら30日不要側へ・出ないなら長期が要る)。危機トリガー(spark=D13)は入れない=観るのは日常からの自然な構造変化。 | **7–14日に短縮**(人数>時間+GPU予算優先。30日は初日安定後の余剰枠で1条件だけ確保を検討。分割実行=10日×3でバイト一致resume可なので予算に合わせて刻める) |
| D16 | **屋内ミクロ観察(indoor系)を本選ランでONにするか**(第58バッチ B0-B9 2026-07-24: 建物内の区画・階・遭遇・会社在席の観測解像度) | OFF(建物粒度のまま=在館は建物単位・室内は不可視=現行) / ON(`indoor.enabled=true`+markov/sfm/meeting/tracks・会社観測は`work.service.ledger/by_org/indoor_fields`。推奨param `sfm.max_substeps=300`/`sample_interval=10`) | **コスト実測**(B9・mock・40体7日→100体30日外挿=`longrun-estimate.md §8`): 推奨paramで per-step +≈0.9s(30日100体で mock 実時間 ~88→~67分。**実LLMでは SFM 計算は LLM レイテンシに隠れ req/s 律速は不変**)・`indoor_tracks_samples` ≈+5MB/30日100体・`org_ledger` は数十KB=無視可・RAM は checkpoint で有界(実測40体7日=339MB)。**得られる観測**: 遭遇ネットワーク(誰と同席=関係形成の前段)/在館(建物・階の占有時系列=🏙タブ)/会社在席(`org_ledger.attendance_min`=🏢タブ)。**spark凍結との整合**: 介入でなく**観測のみ**=会話ペアリング/LLM呼数/L1動力学は不変(k非依存・`tests/test_indoor_invariance.py`+`test_indoor_engine.py`で固定)。tracks OFF でも space_move(L1)は不変=記録と動力学の分離。dt は 0.2 のまま(0.3+ は waypoint 超過で遭遇が偽増=品質劣化・時間短縮は僅少)。 | **OFF(建物粒度で足りる)**。ONは**余剰枠**=SFMコストが実LLMレイテンシに隠れる範囲で観測解像度を上げる追加オプション(遭遇ネットワーク/会社在席を観たい条件だけ ON)。`indoor.enabled=false`=ゴールデンL1バイト一致。長期ランに足すなら推奨param(`longrun30.yaml`(3-b)のコメント)で。 |
| D17 | **endogenous_accept(承諾判断の内生化)条件を本選で回すか**(計画=`docs/plans/endogenous-relations-plan.md` 2026-07-27・実装はユーザー承認待ち) | 回さない(較正確率のまま=現行) / 回す(endogenous_accept {OFF,ON} × k {off, degraded α0.5, free} = 6セル・同一seedペアCRN・14日×seed5/セル=30ラン) | **研究価値**: 「エージェントの主体性は社会構造を動かすか」のtreatment比較=k掃引との交互作用(k↑でONのみ構造が動くか)が本命観測で、それ自体が独立の研究結果になりうる。**コスト**: LLM呼数はON/OFF不変(構造化出力からの決定論抽出=新規呼びゼロ)なので追加コストは条件数×ラン数のみ。**リスク**: sycophancy由来の承諾率上振れは設計で侵入経路を限定済み+乖離KPI(±15pp)をフェーズ移行合否に組込み。**前提**: フェーズ1実装+7日mock予備の完了。**予備実測(第63バッチ 2026-07-27)**: 配線検証mock 12ラン(7日60体・`endogenous_accept_wiring.yaml`)=合計約22分(1ラン94–112秒)で完走し「差なし」を正しく検出(mockエンジン単価。実LLMでは req/s 律速へ変わる=呼数はON/OFF不変なので追加コストは条件数×ラン数のみ)。 | 実装完了を条件に**回す価値が高い**(6セルが予算超過なら k {off, free} の4セルへ縮退)。判断はフェーズ1検収後に確定。 |

**運用手順(初日)**: ①Day-0ベンチ(finals-hardware-plan §2: req/s・c・VRAM・VLM)→ ②D3/D4/D5/D6を確定して観察ラン開始 → ③日1チェックポイントでD9/D10を点検 → ④初日総括でD1/D2/D12を決定(ユーザー判断)→ devlogに記録。

**統合リハーサル実測(2026-07-21・在場1万×1シミュ日 mock・runs/rehearsal_pool10k)— D4/D9/D10 の判断材料**:
- 熟慮 16.8回/人日(設計14-16 ✓)・C2会話 18.2/人日(**目標30比で低め→D10で密度較正**)・
  行間レイヤ全機構発火(寄り道20,997/中断17,652/退屈探索4,871)・イベント16.2M/日・4層parquet健全。
- **初日コールドスタート**: 全員が0時に覚醒状態で開始→初日の朝計画がほぼ出ない(606件のみ=夜勤明け分。
  夜の就寝時内省10,615は全員正常)。2日目以降は正常。**改善案(着席時に自然な睡眠状態で初期化・既定OFF
  ノブ)はユーザー判断待ち**。当面の運用: 観察ランのday1は較正外として扱う。
- **来街者の二重系統**: pool ON でも旧来の日中流入機構が併走(在場1万+流入6,687)。本番の在場曲線
  較正(D4)で「pool一本化 or 流入=意図的な日中churn」を決める。
- RSS ~14.5GB(大半はイベントのRAM保持)→ 本番は日次 checkpoint part flush 必須(既存機構・D8)。

---

## §2 これまでの重要決定の振り返り(時系列)

> 出典: docs/log/devlog-compressed.md(Block #0〜#9)・devlog.md・各設計文書。詳細は参照列から辿る。

| 時期 | 決定 | 理由・効果 | 参照 |
|---|---|---|---|
| 06-29〜 | ゴール階層L0-L4=「動く人工社会」主役+k*研究旗艦。規模ラダー(mock→100-500実LLM→本番) | スケール第一とR²(k)研究の両立 | compressed #0 |
| 06-29〜 | **R1 doctrine**: 新機能=新streamのみ・既定OFF=ゴールデンL1バイト一致・LLM呼数k-blind・no-fingerprint | 決定論・再現性・生得性実験の交絡排除。以後全実装の憲法 | design.md §11 |
| 07-05 | docs記載機能は基本全実装+3D可視化(Blender/Web3D) | 「docsにあるものは実装したい」 | compressed #4 |
| 07-07 | 朝の一日計画を既定ON(全員の標準挙動に) | 一日の骨格=計画駆動へ | simulation.py・compressed #5 |
| 07-12 | **内省全滅バグ発見**(reflect_think=trueでbelief書き戻し0)→旧k*パイロットは再検証要 | 実LLM証拠の信頼性確保 | compressed #6 E6 |
| 07-12 | abliteratedモデル採用(統制目的・セキュリティ4連検証済み) | entropy-reduction除去の対照群 | compressed #6 E8 |
| 07-13 | GitHub**非公開**リポジトリ+三重バックアップ(git/日次mirror/日付zip) | 手動バックアップ頼みからの脱却 | compressed #7 E1-4 |
| 07-15 | 制約付きデコード=format:json既定(従来挙動)・think併用ガード / reflect_max_tokens 768 | KVスロット削減=本選スループット | compressed #7 E8-9 |
| 07-15 | 内省プロンプト4バリアントは**OFF戻し**(丸写し33%=正味悪化) | 「正直な報告>見かけの改善」の実例 | compressed #7 E9 |
| 07-18 | EnvPack 3層分類(共通基盤/共有参照/環境固有)・地名リテラル禁止ガード | 基盤とShibuyaの分離=多都市展開可能(下北沢実証) | compressed #8 E11 |
| 07-18 | PLATEAU実形状化(ハイブリッドglb・既定バイト同一) | 実形状の観察・UE経路 | compressed #8 E13-15 |
| 07-19 | 選挙フル現実化・経済3段(消費/銀行/VC)・ACT-R記憶 | 制度の現実忠実度 | compressed #8 E17-18 |
| 07-19 | **現実忠実再現への転換**: 人数>時間・選挙は名簿制・サービス提供者も全員エージェント・プール=同時滞在の数倍 | 本番の土台=現実渋谷の再現(その上で組織/ファウンダー観察) | realism-first-scale・compressed #8 E19 |
| 07-19 | **全員思考モデルへ転換**(ルールベース専任tier廃止) | 「個々人の思考は全体に関係する」(ユーザー差し戻し)。動的昇格のk*交絡も構造的に解消 | compressed #9 E22 |
| 07-20 | 行間レイヤ承認4件: **C2会話密度=密30/人日・S7=実装するが既定OFF運用・Gumbel採用・3D Phase0はP1同梱** | 推論間の生活の連続性+ゆらぎ+記録に残る会話 | compressed #9 E26・p2-interstitial-design.md |
| 07-20 | W2確認事項承認: 本番=観察ラン・小型モデルA/B実施・保存=前景+サンプル全文・SUMOは公式版 | 本番運用の枠組み | w2-execution-plan.md §7 |
| 07-20 | エンジン実測の書き換え: 旧0.060s/agent-stepは誤帰属→実測+最適化で25万外挿~5-10h/シミュ日=予算圏内 | スケール可否の前提刷新(P1はRAM主眼へ→P3で解決) | devlog Entry 25/27/33 |
| 07-20 | P2 S1-S7+S6b+P3完結: 全員思考+行間レイヤ+100万プール接続(全て既定OFF・バイト一致・1102緑) | 本番アーキテクチャの完成 | devlog Entry 31-33 |
| 07-20 | **視覚=方式F実装・当面Bのみ作動・本番初日に総合判断**(本ファイルD1) | 全員画像は不成立の定量(研究: テキスト観測が画像を上回ることが多い) | agent-vision.md・第39バッチ |

---

## §3 未決のまま初日を迎えてよい事項(参考)

- v2視覚(見えの言語化キャッシュ・注視個人差)=本選後の研究枠。
- 3D移動 Phase 1以降(歩行NW・前景SFM)=本選後。
- オントロジー分化(文化圏×経験の群構成ノブ)=第39バッチで提案中・ユーザー承認待ち。
