# 非定常性診断の事前登録(D0③)

> **ステータス: ドラフト。閾値はユーザー承認(U-10)と実 LLM 診断ラン(GPU 開放初日)を経て確定する。**
> **承認前の変更は自由・承認後は変更禁止。**
>
> 作成: 2026-07-29 / 対象バッチ: レーン1 **D0 非定常性診断+事前登録**
> 上位計画: [twin-physics-vision-affordance-plan.md](twin-physics-vision-affordance-plan.md) §2 レーン1 D0 行・付録 D 節・未決 U-10
> 実装: `scripts/diagnose_stationarity.py`(L1 を読むだけ・シム本体 import ゼロ)/ テスト: `tests/test_diagnose_stationarity.py`
>
> 確定後の運用: 本文書の §3 閾値と §4 判定式は**ラン実行前に固定**され、実行後に変更してはならない。
> 変更が必要になった場合は「変更した」という事実・日付・理由を本文書の §7 変更履歴に追記し、
> 変更前後の判定を**両方**報告する(事後的な閾値いじりを隠さない)。

---

## 前文: 主張の境界(S-04・2026-08-05 追記=**承認対象**)

> **この節は節番号を持たない**(既存 §0〜§7 への本文中の参照を 1 つも壊さないため。
> `docs/research/sv-items-research.md` §6.3 の推奨に従う)。位置が冒頭なのは、
> **査読者が最初に見る箇所に境界を置く**のが S-04 の狙いだからである。
>
> 根拠: Wu, Peng, Ito & Xiao (2025) *LLM-Based Social Simulations Require a Boundary*,
> arXiv:2506.19806(2025-06-24 投稿 / 2026-02-05 改訂)。
> サーベイ側の接続先: `docs/research/llm-social-sim-survey.md` §3 S-04 / S-08 / S-15 / S-17。

### ① 主張する

本研究は、**この設計(実地図・実ダイヤ・IPF 合成人口・全個体一様の認知予算・10 日)の下で**、
世界改変志向の個体(k*)の出現に対する**初期特性の寄与と環境フィードバックの寄与の相対的
大きさ**を主張する。主張は **条件間差が seed 間差を上回った指標に限る**(S-05)。
集団レベルの定性的パターン(規範の成立、組織の自然形成、語の伝播)については、その
成立/不成立と成立条件を主張する。

### ② 主張しない

現実の渋谷において同じ**比率**で世界改変者が現れることは主張しない。個体の行動が特定の
実在個人と一致することは主張しない。得られた協調水準・対立水準の**絶対値**が現実の水準で
あることは主張しない。組織サイズ分布などの**分布形の同定**(Zipf / Gibrat)は N と期間の
不足により主張しない(§3-F の F10 / F11 を参照。**検定できないことを検定できないと書く**のも
この境界宣言の一部である)。

### ③ 外的妥当性の限界

- **(a) モデル 1 系統**: LLM は 1 系統・世代固定であり、`ablate.prompt_paraphrase`(S-16)と
  `ablate.cognitive_tier` が通っても、**別モデルファミリでの再現は主張の外**である
  (Ye, Cao, Chen & Ferrara 2026 *TRAILS*, arXiv:2605.18890 は、同一摂動の効果がモデル間で
  1pp〜76pp と桁違いになることを示した)。
- **(b) 時間尺度**: 期間は 10 日であり、それ以上の時間尺度の主張はしない。
- **(c) 片側解釈(S-15)**: **LLM 社会シムは対立・不満を系統的に過小表現する(既知のバイアス)。
  本ランで観測された協調水準は上限側の推定値であり、対立の実在水準はこれを下回らない。**
- **(d) 分散(S-03)**: §2 の分散比(sim CV / 現実 CV 下界)を必ず併記し、
  **分散比が事前登録の閾値を下回った指標については、主張を集団レベルの定性的パターンに
  限定する**(Wu et al. 基準③)。
- **(e) アウトライアを厚くしない(S-08)**: 我々は分野標準と逆に**アウトライアを優遇しない**
  (全個体一様の認知予算)。これは k* の内生的出現を問うための設計上の選択であり、
  大規模時の効率を代償にしている。`cognitive_tier` ablate は、一様性が結論を作って
  いないことの**片側検査**である。

### ④ 反証条件

次のいずれかが観測された場合、当該仮説を棄却する。

- (a) 条件間差が seed 間差を上回らない。
- (b) `ablate.prompt_paraphrase` の言い換えセットのいずれかで主要結論の**符号が反転**する。
- (c) `ablate.llm_off`(ルール層のみ)で同じ結論が出る = LLM の寄与が無い。
- (d) `experiment.flat_traits`(ゼロ対照)で同じ結論が出る = 特性の寄与が無い。
- (e) `echo_max == 1.000` の崩壊ランが母集団の過半を占める(§1.1 のデータ品質除外)。
- (f) `propagation_off` と本番の中間条件でのみ現れる構造が主結論を担っている
  = 境界アーティファクト(S-17)。

### Wu et al. 2025 の 3 基準と本研究の対応

| Wu et al. 2025 の基準 | 我々の対応 | 状態 |
|---|---|---|
| ① 検証の深さを、研究課題が要求する異質性の水準に合わせる | 研究課題が「裾の創発(k*)」= **異質性要求が最大級** → 分散の検証が必須 | S-03 = `scripts/calibrate_report.py` の個体間分散表で実装 |
| ② 平均の一致と並べて分散を明示的に報告する | `calibrate_report.py` に 個体間 CV / Gini / 上位 10% シェア + **分散比**列 | S-03 で実装 |
| ③ 分散が不十分な場合、主張を集団レベルの定性的パターンに限定する | 本節 ③(d) に規則として固定 | S-04 = 本節で実装 |

> **正直な限界(この表について)**: 現実側の**個体間分散**を直接与える公的統計は極めて少ない。
> 社会生活基本調査は個体間 SD を公表していないため、我々が置ける現実バンドは
> **行動者率 p から復元した CV の下界 √((1−p)/p) という片側バンド**にとどまる
> (`data/battery/reference/estat_activity_rate_cv_lower_bound.json`)。
> **sim の CV がこの下界を下回れば「分散が痩せている」と確実に言えるが、上回っても
> 「十分」とは言えない**。この非対称性は報告のたびに明記する。

---

## 0. なぜ事前登録するか

原指示 D: 「25万体×10日が『饒舌な日常』の反復に収束するリスク」。
本選前に数百体×20 sim日相当で非定常性を測り、**Day 5 が Day 2 と区別できなければ 25万体でも同じ**という
仮説を検証する。閾値を後から決めれば「効果があった」と言える線をいくらでも引けるので、
**ラン実行前に判定式を固定**する。

実査(計画書 §0-6)で判明した重要な訂正: 反復の実体は「7-need の負のフィードバック」ではなく
**EPR(探索/優先的回帰。訪問回数比例の rich-get-richer。`src/society/cognition/routine.py:367`・
Song et al. 2010)+ 決定論日次予定**である。よって診断指標は EPR の回帰率を直接測る形にした。

---

## 1. 指標の定義(数式)

すべて `<run>/l1_events.parquet` のみから計算する。sim 日は **d = ⌊sim_min / 1440⌋**(0 始まり)。

**完全日 / 部分日**: 日 d の sim_min の張り出しを span(d) = max(sim_min|d) − min(sim_min|d) とし、
span(d) ≥ 0.9 · max_{d'} span(d') かつイベント数 > 0 の日を**完全日**とする。ラン開始/終了で切れた
部分日は、ノイズ床・lag 構造・後半窓・既定ペアから除外する(日次系列には残す)。

### 1.1 新規語彙出現率

イベント源: `label_coin` / `vocab_coin`(造語)・`label_adopt`(複雑接触 adopt_threshold=2 での採用)・
`vocab_use`(使用)。item の同定は payload の `item_id`。

- 新語数 **N_coin(d)** = |{ i : i の最初の coin イベントが日 d }|
- 新規採用数 **N_adopt(d)** = |{ i : i の最初の `label_adopt` が日 d }|
- 日 d の使用語彙の台集合 S(d) = { i : 日 d に `vocab_use` された i }
- **新規性シェア** ν(d) = |{ i ∈ S(d) : i ∉ ∪_{d′<d} S(d′) }| / |S(d)|   (S(d)=∅ のとき未定義)

**データ品質除外(第70バッチ追記 2026-07-31)**: L2 `echo_max == 1.000` のラン(=発話が完全な自己反復に
崩壊している)は、語彙系指標が LLM の反復癖のみを測ってしまうため **k* 推定の母集団から除外し「崩壊ラン」として
別掲**する(除外数・条件を必ず報告)。判定は L2 常設の echo 5 列(`src/society/observer/echo.py`)による。
- 使用語彙エントロピー H_V(d) = − Σ_{i∈S(d)} p_i log₂ p_i,  p_i = (日 d の i の使用回数)/(日 d の総使用回数)
- 語彙 Jaccard  J_V(d) = |S(d) ∩ S(d−1)| / |S(d) ∪ S(d−1)|

> **ν(d) が主指標**。「その日交わされた語のうち初めて出た語の割合」。反復する世界では ν → 0。

### 1.2 関係グラフ構造

接触グラフ G_d = (V_d, E_d) は無向・**累積**(日 d の終わりまで)。辺は
`speak`(speaker → payload.hearers の各人)と `dm`(sender → payload.to)から張る
= `scripts/analyze_weak_ties.py:62 build_weighted_graph` と**同一チャネル**(単一の源)。

- 新規紐帯数 **Δ E(d)** = |E_d \ E_{d−1}|
- **新規紐帯率** ρ(d) = Δ E(d) / |E_d|
- 平均次数 k̄(d) = 2|E_d| / |V_d|
- クラスタ係数 C(d) = mean_{v: deg(v)≥2} [ 2 L_v / (k_v (k_v − 1)) ]  (L_v = v の隣接どうしの実辺数。Watts & Strogatz 1998)
- 直径 Diam(d) = 最大連結成分内の全点対最短路の最大値(タイは最小 id で決定論解決)
- 補助: `relation_tier` のペア初出数・`relation_break` の件数(relations が ON のランでのみ非ゼロ)

> C(d) と Diam(d) の定義・実装は `scripts/analyze_weak_ties.py:85 clustering_and_diameter`(第64バッチ)からの
> **コピー**であり、`diagnose_stationarity.py` に純 Python 版として複製した(既存ファイルは変更しない)。
> N ≤ 1500 では等価な numpy 高速路を使い、両者の一致を単体テストで固定している。

### 1.3 行動反復度(ペルソナ逸脱の逆数)

イベント源: `arrive`(payload = `{node, first_visit, name}`)。

- **回帰率** r(d) = 1 − (日 d の `first_visit=true` の arrive 数) / (日 d の arrive 総数)
  = **EPR の優先的回帰が実際に起きた割合**。`first_visit` はシム側が
  `agent.visits[node] == 0` で立てる真値(`scheduler.py:1038-1046`)。
- 訪問エントロピー H_P(d) = − Σ_n q_n log₂ q_n,  q_n = (日 d の ノード n への arrive 数)/(日 d の arrive 総数)
  正規化 Ĥ_P(d) = H_P(d) / log₂ V,  V = ラン全体で到着されたノードの種類数(固定台で日間比較可能にする)
- **日次 Jaccard** J(d) = mean_{a ∈ A(d)∩A(d−1)} |S_a(d) ∩ S_a(d−1)| / |S_a(d) ∪ S_a(d−1)|,
  S_a(d) = エージェント a が日 d に到着したノードの集合

### 1.4 日次自己相似性(**主判定**)

エージェント a・日 d の特徴ベクトルを 2 ブロックで作る。

- **kind ブロック** p^K_{a,d}(κ) = n_{a,d,κ} / Σ_{κ′} n_{a,d,κ′}   (n = イベント種別 κ の件数)
- **visit ブロック** p^V_{a,d}(n) = (a が日 d に n へ到着した回数)/(a の日 d の arrive 総数)
  次元はラン全体の到着回数上位 M=64 ノード + `_other` の 65 次元
- **combined** v_{a,d} = ( ½ · p^K_{a,d} , ½ · p^V_{a,d} )   ⇒ 両ブロックが非空なら ‖v‖₁ = 1

日ペア (t, t′) について、**両日に在席したエージェント** a = 1..n の対応のある差
D_a = v_{a,t} − v_{a,t′} を取り、統計量を

> **TVD(t, t′) = ½ · Σ_j | (1/n) Σ_a D_{a,j} |**

とする(= 平均プロフィール間の全変動距離 ∈ [0,1])。
**「平均的なエージェントの 1 日の活動プロフィールのうち何割が入れ替わったか」**と読める。

**検定**: 帰無仮説「各エージェントにとって日ラベルは情報を持たない」の下では D_a → −D_a が
交換可能。符号 s ∈ {−1,+1}^n に対し T(s) = ½‖(1/n) Σ_a s_a D_a‖₁ とし、両側 p = P( T(s) ≥ T_obs )。

- n ≤ 12: 2ⁿ 全列挙(厳密)
- n > 12: モンテカルロ 10,000 回・**p = (b+1)/(m+1)**(Phipson & Smyth 2010, doi:10.2202/1544-6115.1585)
- 乱数は `numpy.random.default_rng(0)` 固定(シムの `RngHub` は使わない=シム外ツール)
- **交換単位はエージェント**であり、グラフのノード/辺の置換はしない
  (ネットワーク内の非独立性による過大有意を避ける。Farine & Carter 2022, doi:10.1111/2041-210X.13741)
- 実装作法は `scripts/analyze_endo_treatment.py:203 sign_flip_test` を踏襲した多変量版

**補助効果量**: 次元ごとの対応のある Cohen's dz = mean_a(D_{a,j}) / sd_a(D_{a,j}) の |·| 平均・最大。

**ノイズ床**: F = median{ TVD(d, d+1) : d, d+1 とも完全日 }。
**比** R(t,t′) = TVD(t,t′) / F(= 隣接日どうしの揺らぎの何倍離れているか。スケールフリー)。

**lag 構造**: 完全日どうしの全ペアを lag = |t − t′| で束ね、lag ごとの平均 TVD と、
lag に対する OLS 傾き **lag_slope** を出す。定常なら平坦(slope ≈ 0)、ドリフトがあれば正。
lag 2..14 で平均 TVD が最小になる **lag_min** は周期構造の指紋(7 なら週次リズム)。

**立ち上がり(burn-in)の終端推定** burn_in_end = max( 各指標が飽和側へ入りきる最初の完全日 )
(ν < ν_min / ρ < ρ_min / r > r_max の 3 本すべてが到達した場合のみ定義)。

**過渡 vs 常態の切り分け(late_same_lag)**: 主判定ペアと**同じ lag** を、完全日の後ろ 40% の中で
取り直したペア。主判定が PASS でこちらが FAIL なら、非定常性の正体は**蓄積の過渡**であって
日常の非反復性ではない。

### 1.5 指標定義補遺: 伝播チャネルの**三層**宣言(S-12・2026-08-06 追記=**承認対象**)

> **この節が解く問題**: §1.2 は接触グラフを「`speak` と `dm` を同一チャネルとして 1 つのグラフに
> 合成」と定義している。これはノイズ床 F の算出には十分だが、**「規範はどの層で成立するか」**を
> 問うには層を分けねばならない(Centola & Macy 2007 の複雑伝染 / Szell et al. 2010 の multiplex)。
> 本節は §1.2 の定義を**置き換えない**(§1.2 は診断用の合成グラフとしてそのまま凍結)。
> 層別グラフは `scripts/analyze_layers.py` の別指標として新設し、その定義をここで事前に固定する。

**実査結果**: `transmission` イベントの payload には既に `channel` 欄がある
(`src/society/observer/schema.py` の `transmission` 登録行)。
`scripts/analyze_layers.py` は次の**三層**に写像する。**二層(offline / online)では尽くせない**。

> **★ 2026-08-06 訂正(実測による)**: リサーチ文書 `docs/research/sv-remaining-items-research.md`
> §1.4 は `channel` の実値を **5 種**(`face` / `dm` / `sns` / `search` / `news`)としていたが、
> **既存 95 ランの `transmission` を全走査した結果、6 種目 `event` が実在した**
> (実測 `face` 257,043 / `sns` 421,637 / `search` 162,339 / `event` 43,500 / `news` 7,876 /
> `dm` 256)。出所は `src/society/tools.py` L1085–1087 で、**催しのホストが参加者へ語を教える**
> 経路である(`_hear_words(..., host.id, "event", ...)` = 送り手は**実在の人**)。
> リサーチ文書の 5 種表は `scheduler.py` の `_hear_words` 呼び出しのみを数えており、
> `tools.py` 側の 1 か所を落としていた。**`event` を落とすと offline 層の伝播量を
> 実測で約 14%(43,500 / 300,543)過小評価する**ため、本表で層を確定する。
> なお **`dm` チャネルは実測で 256 件(全体の 0.03%)しかない**。「online 層 = DM 主体」と
> いう直感は**実データでは成立しない**(online の実体はほぼ SNS である)。この非対称も報告する。

| channel | 送り手 | 本事前登録での層 | 理由 |
|---|---|---|---|
| `face` | 人 | **offline**(対面) | 同席が要る。クラスタが密で補強が起きやすい |
| **`event`** | **人(催しのホスト)** | **offline**(対面) | ★**追加**。催しへの同席が要る = `face` と同じ物理条件 |
| `dm` | 人 | **online**(対人) | 1 対 1・遠隔 |
| `sns` | 人 | **online**(対人) | 1 対多だが送り手は人 |
| `search` | **−1(人でない)** | **broadcast**(媒体) | 対人ネットワークの辺にならない |
| `news` | **−1(人でない)** | **broadcast**(媒体) | 同上 |

**この三層目(broadcast)の存在自体が報告に書く価値のある観測である**
(サーベイ §5.1.2 は offline / online の二分しか提示していない)。

**★ 層の帰属は `from` 欄の実測で検算する(宣言だけで済ませない)**: `analyze_layers.py` は
チャネルごとに「`from == -1` の割合」を出力し、**offline / online に分類した channel で
`from == -1` が出たら警告を立てる**(= 本表の前提が壊れたことの機械検知)。
**未知の channel が現れた場合は `unmapped` として別掲し、勝手にどれかの層へ入れない。**

**層別グラフの辺の定義(事前固定・ラン後に変えない)**

**★ グラフは 2 系統を別々に作り、混ぜない**(混ぜると「何を測ったか」が言えなくなる):

**(A) 相互作用グラフ**(誰と誰が接触したか。`transmission` の有無に依存しない)

- **offline 層**: `speak`(speaker → hearers の各人)。無向・重み = 共起回数。
- **online 層**: `dm`(sender → to)+ `sns_read`(reader → payload.authors の各人)
  + `sns_like` / `sns_reshare`(反応者 → author)。
  ★**`sns_read` 由来の辺を online 層に入れる**ことをここで宣言する。理由: 入れないと
  online 層が DM のみになり、SNS の到達が 1 本も辺として数えられない(= online 層の
  過小評価)。**実測で `dm` チャネルが全伝播の 0.03% しかないことが判明した以上、
  この判断は「あった方が良い」ではなく「入れなければ online 層が実質空になる」**。
  `scripts/analyze_weak_ties.py` の合成グラフ(= §1.2)は **DM のみ**のままであり、
  **本節は §1.2 を変更しない**。両者は別の量である旨を報告に明記する。
- **broadcast 層**: 辺を張らない。**到達件数(何人が何回受け取ったか)としてのみ数える**。
  `sns_read` / `sns_like` / `sns_reshare` の相手が **`-1`(媒体の投稿)** の分と、
  `news_read` はここへ計上する。送り手 id が `-1` である以上、対人グラフの辺として
  数えるのは誤りである。

**(B) 伝播グラフ**(何が実際に伝わったか。`transmission` の `channel` で層を決める)

- 辺は `(payload.from → agent_id)`。層は上表の写像。**`from == -1` の行は辺にせず
  broadcast の到達件数へ**。`event` は offline、`sns` / `dm` は online。
- (A) と (B) は**別の量**である。(A) は「話しかけた」、(B) は「語が乗った」。
  両者の辺集合の重なり(edge overlap)を出力し、**冗長なら冗長と報告する**。

**共通規約**

- コミュニティ検出を層別に行う場合は `society.observer.measure.communities()`(決定論 LPA)を
  **層別の合成イベント列に対して呼ぶ**(`scripts/analyze_communities.py` の
  `_synthetic_events` と同じ流儀)。**凍結ファイルは 1 バイトも変更しない**。
- **層間 edge overlap**(層 α に辺があるとき層 α' にも辺がある条件付き確率)を
  multiplex の標準量として併記する(Szell et al. 2010)。

**この宣言が §3-F の F7 に及ぼす影響**: F7(次数分布の重い裾)を**層別**に判定する。
判定枠は F7 のまま(主判定・条件付き = `relations.dunbar` OFF の条件に限る)とし、
**offline / online の 2 層それぞれで裾を推定する**(broadcast は辺を持たないので F7 の対象外)。
**両層とも裾が出た場合にのみ F7 を「再現できた」と数える**のではなく、**層ごとに合否を出して
併記する**(層で結果が割れること自体が multiplex の知見であるため)。

**事前の予測(ラン前に書く = 事後解釈を封じる)**: Centola & Macy 2007 に従えば、
**offline 層は密で補強が起きやすいので規範(複雑伝染)が成立しやすく、online 層は到達が広いので
語(単純伝染)が速く広がる**。この予測が外れた場合も、外れたと報告する。

---

## 2. タスク2の実測値(mock・2026-07-29 実施)

**結論(先出し)**: `daily300_100d` = **TRANSIENT_ONLY**(立ち上がり day 0-18 は非定常だが、
その後は週次周期を持つ**定常**過程)/ `endo7_kfree_eoff_s11` = **STATIONARY_LIKE**(7 日では
Day2 と Day5 は隣接日どうしと同程度にしか離れていない)。
**原指示 D の危惧は mock 条件では当たっている**が、その正体は「饒舌な日常」というより
**語彙と関係グラフの飽和**(100 日で 300 人の接触グラフはクラスタ係数 0.97・平均次数 290/299 の
ほぼ完全グラフになる)である。

コマンド:
```
python scripts/diagnose_stationarity.py runs/daily300_100d
python scripts/diagnose_stationarity.py runs/endo7_kfree_eoff_s11
```
出力は各ランディレクトリの `diagnose_stationarity.json` / `.md`。

**読み込み戦略(11.9M 行への対処)**: `pyarrow.parquet.ParquetFile.iter_batches` の **2 パス** ストリーム走査。
パス1は列 `[sim_min, agent_id, kind]` のみ(payload に触れない=最安)で kind 語彙・エージェント id 集合・
日数を確定。パス2で `payload` を加え、**JSON パースは必要な kind の行だけ**
(`pc.is_in` マスク → `filter` → その行だけ `to_pylist`)。11,915,782 行のうち JSON パース対象は
1,267,164 行(10.6%)。特徴量は `np.bincount` で密配列へ直接畳み込む(Python ループ無し)。
`society.observer.measure.load_events`(全件 RAM 展開)は**使わない**。
実測: **64.6 秒・Python ヒープ peak 149.5 MB**(`tracemalloc`。pyarrow の C++ バッファは含まない)。

### 2.1 `runs/daily300_100d`(300体 × 100 sim日 mock・11,915,782 イベント・250MB)

ラン設定の要点: `k.writeback=free` / `calendar.enabled=true`・`weekday_work=true`(start_date 2026-07-09)/
`relations.enabled=true` / `labeling.mode=constrained`・`adopt_threshold=2` / `commerce.enabled=true` /
`weather.enabled=true` / `schedule.enabled=true`。
検出された日数 101(うち完全日 99。day 0 と day 100 が部分日)。

**日次系列(抜粋)**

| day | 新語 | 新規性 ν | 新規辺 ΔE | 新規紐帯率 ρ | 平均次数 | クラスタ係数 | 直径 | 回帰率 r | 訪問Ĥ | 前日 Jaccard |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0* | 314 | 1.000 | 2802 | 1.000 | 19.1 | 0.274 | 5 | 0.226 | 0.802 | — |
| 1 | 220 | 0.513 | 2978 | 0.515 | 38.7 | 0.311 | 4 | 0.397 | 0.784 | 0.210 |
| 2 | 202 | 0.393 | 3857 | 0.400 | 64.2 | 0.353 | 3 | 0.425 | 0.812 | 0.133 |
| 5 | 114 | 0.212 | 1541 | 0.100 | 102.8 | 0.456 | 3 | 0.538 | 0.792 | 0.170 |
| 9 | 67 | 0.107 | 1939 | 0.093 | 139.2 | 0.558 | 2 | 0.539 | 0.803 | 0.100 |
| 10 | 59 | 0.089 | 1349 | 0.061 | 148.2 | 0.580 | 2 | 0.538 | 0.815 | 0.101 |
| 20 | 22 | 0.030 | 567 | 0.019 | 199.2 | 0.715 | 2 | 0.642 | 0.794 | 0.150 |
| 50 | 2 | 0.002 | 148 | 0.004 | 265.1 | 0.898 | 2 | 0.728 | 0.791 | 0.149 |
| 95 | 2 | 0.002 | 27 | 0.001 | 290.0 | 0.971 | 2 | 0.799 | 0.794 | 0.082 |

`*` = 部分日。全 101 日の系列は `runs/daily300_100d/diagnose_stationarity.json` の `daily[]`。

**自己相似性**

| ペア(combined) | n | TVD | R = TVD/F | p | dz平均 |
|---|---:|---:|---:|---:|---:|
| Day 2 vs Day 5 | 300 | **0.1107** | **2.25** | 0.0001 | 0.131 |
| Day 2 vs Day 20 | 300 | 0.1193 | 2.43 | 0.0001 | 0.156 |
| Day 2 vs Day 99 | 300 | 0.1257 | 2.56 | 0.0001 | 0.166 |
| Day 96 vs Day 99(late_same_lag) | 300 | **0.0479** | **0.98** | 0.0002 | 0.067 |
| Day 98 vs Day 99(隣接) | 300 | 0.0410 | 0.83 | 0.0011 | 0.059 |

- ノイズ床 **F = 0.0491**(完全日の隣接ペア 98 本の中央値。最大 0.1241)
- lag 傾き **lag_slope = 0.00041**(ほぼ平坦)
- **lag_min = 7**(lag 2..14 で平均 TVD 最小 = 0.0490。lag 1 は 0.0633)
  → **週次リズムを正しく検出している**。`calendar.enabled=true`・`weekday_work=true` の指紋であり、
  診断ツールの妥当性チェックにもなっている(**周期は非定常性ではなく定常な周期構造**)。
- **burn_in_end_estimate = 18**(ν<0.05 到達 day 16 / ρ<0.02 到達 day 18 / r>0.90 は 100 日でも未到達)

**ドラフト閾値での判定 = `TRANSIENT_ONLY`**(primary PASS / sustained FAIL)。

**★最重要の観察**: Day2 vs Day5 は p=0.0001・TVD=0.111・R=2.25 で「区別できる」が、
**同じ lag 3 を後期で取り直した Day96 vs Day99 は TVD=0.048・R=0.98 = ノイズ床と同等**。
つまり `daily300_100d` の非定常性は **day 0〜18 の蓄積の過渡**であって、常態は
「週次周期を持つ**定常**過程」である。原指示 D の危惧(「饒舌な日常の反復に収束する」)は、
**mock・300体・100日という条件では day 18 以降について当たっている**。

**★本選 10 日ランに直結する観察**: burn-in が day 18 まで続くので、**10 sim日ランはほぼ全部が過渡**。
day1〜10 の窓に限って再計算すると F = 0.0557・TVD(2,5) = 0.1107(R = 1.99)・
late_same_lag に相当する TVD(6,9) = 0.0984(R = 1.76)で、**両方が主判定を通る**。
すなわち **10 日ランでは「非定常」判定はほぼ自動的に出る**。この事実を承知の上で閾値を設計する必要がある(§3-B)。

10 日窓での構造 3 指標(day 6-10 平均): ν = 0.134 / ρ = 0.070 / r = 0.562。

### 2.2 `runs/endo7_kfree_eoff_s11`(60体 × 7 sim日 mock・110,812 イベント)

検出日数 8(うち完全日 7。day 7 が部分日 = 1,156 イベントのみ)。

| day | 新語 | ν | ΔE | ρ | 平均次数 | クラスタ係数 | 直径 | r | Ĥ | 前日 Jaccard |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 70 | 1.000 | 91 | 1.000 | 3.64 | 0.275 | 8 | 0.266 | 0.791 | — |
| 2 | 69 | 0.489 | 122 | 0.370 | 11.00 | 0.379 | 4 | 0.530 | 0.766 | 0.198 |
| 5 | 44 | 0.275 | 113 | 0.172 | 21.90 | 0.494 | 3 | 0.543 | 0.797 | 0.132 |
| 6 | 46 | 0.269 | 85 | 0.115 | 24.73 | 0.523 | 3 | 0.556 | 0.800 | 0.122 |
| 7* | 3 | 0.150 | 1 | 0.001 | 24.77 | 0.523 | 3 | 0.806 | 0.550 | 0.081 |

| ペア(combined) | n | TVD | R | p |
|---|---:|---:|---:|---:|
| Day 2 vs Day 5 | 60 | 0.1282 | 1.05 | 0.0001 |
| Day 2 vs Day 6 | 60 | 0.1361 | 1.11 | 0.0001 |
| Day 3 vs Day 6(late_same_lag) | 60 | 0.1375 | 1.12 | 0.0001 |
| Day 5 vs Day 6(隣接) | 60 | 0.0910 | 0.74 | 0.0058 |

- ノイズ床 **F = 0.1226**(N=60 なので daily300 の 2.5 倍。**F は N に強く依存する** = 絶対 TVD 閾値を
  そのまま横断適用してはならない根拠)
- lag_slope = 0.0058 / lag_min = 2(7 日しかないので週次は検出しようがない)
- burn_in_end_estimate = **None**(7 日では 3 指標のいずれも飽和側に届かない)
- ドラフト閾値での判定 = **STATIONARY_LIKE**(R = 1.05 < 1.5 で FAIL)

**解釈**: 60体7日では TVD の絶対値は大きい(0.128)が、それは**隣接日の揺らぎも同じくらい大きい**からで、
Day2 と Day5 は隣接日どうしと同程度にしか離れていない。n が小さいほどノイズ床が上がるので、
**絶対 TVD ではなく比 R を主軸に据える**という設計判断はここで裏付けられた。

### 2.3 実測から得た設計上の教訓(閾値に反映済み)

1. **F は N に強く依存**(N=300 で 0.049 / N=60 で 0.123)→ 主軸は絶対 TVD ではなく **比 R**。
2. **n が数百あると p はほぼ必ず 0.05 を切る**(daily300 の隣接日ですら p=0.0011)
   → **p 単独では判定に使えない**。p と効果量の**連言**が必須。
3. **burn-in(蓄積の過渡)と常態を分けないと 10 日ランは自明に「非定常」になる**
   → `late_same_lag` 条件を判定式に入れる(§4-B)。
4. **周期構造(lag_min=7)は非定常性ではない** → 判定時に必ず併読する。

---

## 3. 閾値の提案(ドラフト)

すべて `scripts/diagnose_stationarity.py` の `DRAFT_THRESHOLDS` に同値で埋め込んである
(CLI 実行で機械判定が出る)。**承認までは変更自由・承認後は変更禁止**。

### A. 主判定(日次自己相似性・combined ブロック・主判定ペア = Day 2 vs Day 5)

| # | 条件 | 閾値 | 根拠 |
|---|---|---|---|
| A1 | 置換検定 **p < 0.05** | 0.05 | 慣行。ただし単独では効かない(教訓2) |
| A2 | 効果量 **TVD ≥ 0.05** | 0.05 | daily300 のノイズ床 0.049 とほぼ同値 = 「N=300 規模で少なくともノイズ床 1 個ぶんは動く」下限。F ≈ 0 の退化ケースへの保険 |
| A3 | 比 **R = TVD/F ≥ 1.50** | 1.50 | 実測 R: daily300 の (2,5)=2.25 / (96,99)=0.98 / (98,99)=0.83、endo7 の (2,5)=1.05。「常態の日間揺らぎ」は R ≈ 0.8-1.1 に集まる。その上側に 1.5 を置けば常態を非定常と誤判定しない |

**主判定 = A1 ∧ A2 ∧ A3**。

### B. 過渡切り分け(**burn-in 対策。10 日ランでは実質これが本体**)

| # | 条件 | 閾値 | 根拠 |
|---|---|---|---|
| B1 | `late_same_lag` ペア(主判定と同じ lag を完全日の後ろ 40% で取り直したもの)が A1∧A2∧A3 を満たす | A と同値 | daily300 で (2,5) PASS / (96,99) FAIL = 過渡と常態の区別に実際に効いた |

### C. 構造 3 指標(補助。後半窓 = 完全日の後ろ半分の平均)

| # | 条件 | 閾値 | 根拠 |
|---|---|---|---|
| C1 | 語彙新規性 **ν̄_late ≥ 0.05** | 0.05 | daily300 の ν は day16 に 0.05 を割り、後半平均 0.0019(明確な飽和)。endo7 は 0.333 |
| C2 | 新規紐帯率 **ρ̄_late ≥ 0.02** | 0.02 | daily300 は day18 に 0.02 を割り後半平均 0.0019。endo7 は 0.182 |
| C3 | 回帰率 **r̄_late ≤ 0.90** | 0.90 | daily300 は 100 日でも 0.75-0.80 で 0.90 に届かない。0.90 = 「10 回に 9 回は既訪問」= EPR がほぼ完全に閉じた状態 |

C は**ゲートではなく報告**。合否は N・日数・conf(relations/labeling の ON/OFF)に強く依存するため。

### D. 併読必須の派生量(閾値なし・必ず報告する)

- `burn_in_end_estimate`(3 指標が飽和側に入りきる日)
- `lag_slope`(TVD の lag 依存の OLS 傾き)
- `lag_min`(周期構造の指紋。7 なら週次)
- ノイズ床 F と各ペアの R

### E. 規範成立の判定と下方因果(第74バッチ接続・2026-07-31 追記=承認対象)

`scripts/analyze_norms.py` は閾値未指定でエラーになる設計(既定値を埋め込まない)。承認対象の推奨値:

| # | 条件 | 推奨値 | 根拠 |
|---|---|---|---|
| E1 | 規範候補 = 到達ステージ **≥ 3**(指示詞化=「例の◯◯」で通じる共有参照の成立) | `--norm-stage 3` | stage2(他者引用)は単なる伝播で規範性を含まない。stage4 のみだと候補が稀少すぎて検定不能リスク |
| E2 | 使用者数 **≥ 3 名**(相異なる話者) | `--norm-threshold 3` | 複雑伝染の採用閾値(adopt_threshold=2)+1 = 「ペアの内輪語」を規範候補から除外する最小値 |
| E3 | 頑健性: stage **≥ 4**(合意参照)でも同方向の効果 | 副判定 | 主判定が表層検出の緩さに依存しないことの確認 |
| E4 | コホート最小 **n ≥ 10/群**+`--require-full-day` | — | 初日欠落個体の混入を排除。満たさない場合は「判定不能」と報告(有意でも主張しない) |
| E5 | 検定 = ラベル置換 **p < 0.05**(analyze_norms.py の置換検定) | 0.05 | A1 と同じ慣行。**語の可用性交絡**(成立後コホートほど語に接触しやすい)は第74実装の注意書きどおり必ず併記し、単独で「下方因果」と断定しない |

**データ品質除外(§1.1 記載)**: L2 `echo_max == 1.000` のランは崩壊ランとして k*/規範推定の母集団から除外・別掲。

### E'. 規範 4 ステージと先行研究のライフサイクルの対応(S-13・2026-08-06 追記=**承認対象**)

> **この節が解く問題**: 本シムは規範の成立を 4 段(`none` / `coin` / `quote` / `definite` /
> `institution`)で観測するが、**この 4 段が先行研究のどの段に対応するのかを事前に宣言していない**。
> 宣言しないまま結果を出すと、事後に都合の良い段へ読み替えられる。
>
> **★実装は 1 バイトも触らない。** 4 段の定義は `src/society/observer/norms.py` の
> `STAGE_NAMES` にあり、同ファイルと `scripts/analyze_norms.py` は `metrics_spec_hash` の
> 凍結対象(`SPEC_FILES`)である。**コメント 1 文字・docstring 1 文字でもハッシュが動く**ため、
> 対応表は**本事前登録に置く**(サーベイが提案した「docstring に 1 表」は採らない)。

| 本シム | 定義(実装) | Ren et al. 2024(**CRSEC**) | Finnemore & Sikkink 1998(norm life cycle) | **測っていないもの(正直な注記)** |
|---|---|---|---|---|
| **S1 `coin`** | `label_coin` イベント。同語の**最初の coin だけ** | **C**reation(規範の創出) | **Emergence**(norm entrepreneur による創出) | coiner が「規範を作ろうとしたか」の**意図は測らない**(行動からの検出のみ) |
| **S2 `quote`** | coiner **以外**による最初の使用(`transmission` 経由 or 発話テキスト初出の早い方) | **S**preading(伝播) | **Emergence → Cascade の境界**。F&S は**約 1/3 の採用**を tipping point とする | 本シムの S2 は**1 人目**で成立する = tipping point **ではない**。採用率の閾値は §3-E の **E2(使用者 3 名)**が別途担う |
| **S3 `definite`** | 発話中に「〈定冠詞相当マーカー〉+ 語」(「例の◯◯」)が現れた初出 | **R**epresentation(形式的表象) | **Cascade**(共有指示対象の成立) | 表層文字列一致のみ = **過小検出**。マーカーは `conf` が唯一の源で、未設定なら永久に 0 件 |
| **S4 `institution`** | 発話中に〈合意参照マーカー〉と語が**同一発話内に共起**した初出 | **E**valuation + **C**ompliance(健全性検査と遵守) | **Internalization**(遵守が自動化された状態) | ★**最重要の限界**(下記) |

**★ S4 についての限界宣言(この 1 行を書かないと過大主張になる)**:
> F&S の internalization は**行動の自動化**であり、CRSEC の Compliance は**計画・行動への
> 組み込み**である。しかし**本シムの S4 が見ているのは「言及」だけ**である。
> **「規範に従って行動した」ことは測っていない。** したがって S4 到達を
> 「規範が内面化された」と読んではならず、「**合意への言及が発話中に現れた**」までしか言えない。

**位置づけの 1 行(Hollander & Wu 2011)**: 規範的 MAS は規範を **rigid**(全体制約として強制)と
**flexible**(遵守/違反をエージェントの決定に委ねる)に分けられる。**本シムは flexible 側**である
(規範に反する行動を機構で禁じていない)。この位置づけを報告に明記する。

- Ren, S., Cui, Z., Song, R., Wang, Z. & Hu, S. (2024). *Emergence of Social Norms in Generative
  Agent Societies: Principles and Architecture.* IJCAI 2024 / arXiv:2403.08251.
- Finnemore, M. & Sikkink, K. (1998). *International Norm Dynamics and Political Change.*
  International Organization 52(4):887–917.
- Hollander, C. D. & Wu, A. S. (2011). *The Current State of Normative Agent-Based Systems.*
  JASSS 14(2):6. DOI 10.18564/jasss.1750.

### F. 渋谷の stylized facts(S-02・2026-08-05 追記=**承認対象**)

正典: `docs/research/sv-items-research.md` §1 / サーベイ `docs/research/llm-social-sim-survey.md` §3 S-02。

**この節が解く問題**: `scripts/calibrate_report.py` の `REALITY` 表(29 エントリ)が持つのは
現実の**水準**(乗降人員・睡眠時間・失業率など)の照合であって、**法則の再現**ではない。
**S-02 が足すのは「水準」ではなく「形」の判定である。**
ラン前に「再現できたら成功と見なす法則」と「再現できなくても主張に影響しない法則」を
分けて固定しないと、事後にどの数字でも「合っている」と言えてしまう。

**循環性リスクの列について**: 本シムは EPR(exploration & preferential return)を
`src/society/cognition/routine.py` に実装済みで、日次計画も決定論的に与えられる(§0 に明記済み)。
したがって**移動系の法則の一部は「入力の再生」であり、再現できても創発の証拠にならない**。
この列で区別する。

**ODD との接続**: ODD 2020 の要素①は "Purpose **and patterns**" であり、"patterns" とは
「モデルが再現できたら成功と見なすパターン」の**事前宣言欄**そのものである。
本節は独自発明ではなく **ODD 要素①の充足**である(`docs/plans/observation-report-template.md` §1)。

#### F.1 候補表

| # | 法則 | 定量形 | 出典(一次) | シムで検定可能か | 循環性 | 検定方法 | 判定枠 |
|---|---|---|---|---|---|---|---|
| F1 | 時間帯別人流の二峰性 | 在圏人口の時刻系列に朝(7–9h)・夕(17–19h)の 2 極大 | 東京都市圏 PT 調査 第 6 回 / 全国の人流オープンデータ(1km メッシュ・昼夜) | ◎(1 日 288 step で可) | **高**(日次計画が入力) | L1 移動イベント → 時刻別在圏人口。極大 2 個 + 谷/ピーク比 < θ | **参考** |
| F2 | 訪問頻度の Zipf 則 | f_k ~ k^(−ζ), ζ ≈ 1.2 | Song, Koren, Wang & Barabási 2010 (Nature Physics 6:818) | ○(10 日・1 人あたりランク 5–15 程度) | **高**(EPR 実装済み) | 個体別の訪問地点をランク付け、両対数回帰で ζ 推定。**実装パラメータから予測される ζ との一致 = 内部整合性検査**として使う | **参考**(内部整合性のみ) |
| F3 | 探索の逓減 | S(t) ~ t^μ, μ = 0.6 ± 0.02 | Song et al. 2010 | △(t レンジ 1–2880 step で両対数の腕が短い) | **高** | 個体別の累積訪問地点数 S(t) の両対数傾き | **参考** |
| F4 | 滞在時間分布の重い裾 | 指数分布が棄却され、truncated power law / lognormal が優越 | 人流実証文献群。※分布形は集計水準に依存する点に注意 | ◎ | 中(POI 営業時間・行動長で上限が入る) | 滞在時間の対数尤度比検定(指数 vs 対数正規 vs 切断冪)。**「指数分布の棄却」までを主張し、どの裾かは主張しない** | **主判定(弱形)** |
| F5 | 行動のバースト性 | 事象間隔が heavy-tail。burstiness B=(σ−m)/(σ+m) > 0 | Barabási 2005 (Nature 435:207) / Goh & Barabási 2008 (EPL 81:48002) | ◎ | **低**(発話タイミングは内生) | 個体別の発話・DM の事象間隔から B と memory M を算出。Poisson 帰無(B≈0)を置換検定で棄却 | **主判定** |
| F6 | 対面接触時間の重い裾 | 接触継続時間・個体別総接触時間・接触間隔がいずれも heavy-tail | Cattuto et al. 2010 (PLOS ONE 5(7):e11596) / SocioPatterns。文脈(会議・学校・病院)に依らず同型 | ◎ | **低**(接触は空間から内生) | `speak` の hearers から接触ペア×継続を再構成し、3 分布とも指数を棄却 | **主判定** |
| F7 | 社会ネットワーク次数分布の重い裾 | 次数分布が右に長い裾(Pareto 的) | 同上 + 影響の Pareto / Matthew(サーベイ §5.1.3) | ○ | 中 | offline(対面 speak)/ online(SNS・DM)を**分離して**次数分布を推定(S-12 と接続) | **主判定(条件付き)** |
| F8 | 週次周期 | 平日/休日プロフィールの差 | 社会生活基本調査(週全体/土曜/日曜の別表) | △(10 日 = 週末 2 回) | 高(カレンダーが入力) | 平日/休日の日次プロフィール差が **seed 間分散を超えるか**(S-05 の分散分解を流用) | **参考** |
| F9 | 混雑と待ちの単調関係 | 密度 ↑ → 待ち時間 ↑ の単調性 | 都市工学の一般則(本プロジェクトは既に環の閉じを L1 実例で実証済み) | ◎ | 高(機械系) | 密度ビン別の平均待ち時間の単調性(Spearman ρ > 0) | **参考**(機械系の健全性確認) |
| F10 | 企業規模の Zipf 則 | P(S>s) ∝ s^(−1) | Axtell 2001 (Science 293:1818) | **×** | — | 自然形成組織が 10 日で数十件規模。Axtell は 550 万社。**N が 4–5 桁足りない** | **不採用**(検定不能) |
| F11 | Gibrat 則(比例効果) | 成長率が規模に独立 | Axtell 2001 の Kesten 過程解釈 | **×** | — | 10 日では成長率の推定に必要な時間断面が足りない | **不採用**(検定不能) |
| F12 | 移動の予測可能性 | 上限 ~93% | Song, Qu, Blumm & Barabási 2010 (Science 327:1018) | △ | 高 | 実エントロピー推定が 10 日では不安定 | **参考** |

#### F.2 二分(**ラン前に固定する。ラン後に足さない/外さない**)

| # | 枠 | 対象 | 意味 | 根拠 |
|---|---|---|---|---|
| F-A | **主判定**(再現できたら成功と見なす) | **F5 / F6 / F4 / F7(条件付き)** の 4 件 | この 4 件が出れば「現実の人間行動の法則を再現した」と主張してよい | 選定理由が明快である: **いずれも入力に埋め込まれていない**(タイミングと接触は空間・スケジューリングから内生する)。F1–F3 のような「EPR を入れたから EPR 則が出た」という循環に落ちない |
| F-B | **参考**(再現できなくても主張に影響しない) | F1 / F2 / F3 / F8 / F9 / F12 | 出れば内部整合性の傍証。出なくても結論を変えない | いずれも循環性が高い(日次計画・EPR・カレンダー・機械系が入力側にある)か、10 日では推定が不安定 |
| F-C | **不採用**(検定不能と明記する) | F10 / F11 | **検定しない。** 「検定できない法則を検定できないと書く」こと自体が前文「主張の境界」②の一部である | N が 4–5 桁足りない(Axtell 2001 は 550 万社)/ 10 日では成長率の時間断面が足りない。書かないと「都合の良い法則だけ選んだ」と読まれる |

#### F.3 F7 の条件(**事前に宣言する**)

**`relations.dunbar` が ON のランでは、社会ネットワークの次数分布の裾は最外層 51 で
機械的に切られる**(`ablate` ではなく `relations.dunbar` の層サイズ 5/15/50/150 × scale 0.34 の
最外層拘束による)。したがって:

- **F7 を主判定に用いるのは `relations.dunbar` が OFF の条件に限る。**
- ON の条件で裾が切れていることは「モデルが裾を作れなかった」ではなく「上限を課した」
  結果であり、**再現の失敗として数えない**(この宣言を事後に持ち出さないために事前登録する)。

#### F.4 主張は「水準」ではなく「形」である(1 行)

**F1 以外は渋谷固有ではなく人間行動の普遍則である。** 渋谷固有の水準値(乗降人員・昼間人口・
滞留人口)は既に `scripts/calibrate_report.py` の `REALITY` 表が持っており、
**本節が足すのは「水準」ではなく「形」(分布形・裾・周期・単調性)の判定である。**

#### F.5 正直な注記(**数値を捏造しない**)

- **F2 の ζ ≈ 1.2 と F3 の P_new = ρ·S^(−γ) の ρ, γ は一次未確認である**
  (`docs/research/sv-items-research.md` §8-1: Song et al. 2010 の本文 PDF がテキスト抽出
  不能だった。μ = 0.6 ± 0.02 のみ二次で確認済み)。**F2 / F3 を実際に検定するときは
  本文 PDF から数値を取り直すこと。** 取り直せない場合は「一次未確認のため数値との
  一致は主張せず、両対数の傾きの符号と概形のみ報告する」に留める。
  ただし F2 / F3 は**参考枠**なので主判定には影響しない。
- **F1 の二峰性の定量形(谷/ピーク比の閾値 θ)も一次数値を取っていない。**
  東京都市圏 PT 調査 第 6 回の概要が「朝ピークは横ばい、昼以降減少」と述べる程度までしか
  確認できていない。θ はラン前に別途固定するか、F1 を「極大が 2 個あるか」の定性判定に
  限定する(こちらを既定とする)。
- **Bisbee et al. 2024 の書誌(掲載誌・巻号)は一次未確認**であり、分散の議論の根拠として
  引くときは書誌を確定してから引く。
- **データ品質除外は F 節にも適用する**: L2 `echo_max == 1.000` の崩壊ランは、F5(発話の
  バースト性)と F6(接触時間)の母集団から除外し別掲する(§1.1 と同じ規則)。

### G. seed 間分散の分散分解(S-05・2026-08-06 追記=**承認対象**)

> **この節が解く問題**: 前文 ④ 反証条件 **(a)「条件間差が seed 間差を上回らない」**は既に
> 承認対象として書かれているが、**その判定を実際に下す装置と規則が定義されていない**。
> 装置が無ければ反証条件そのものが判定不能であり、事後にどうとでも読める。
> **本節は「あると良い装置」ではなく、既に約束済みの判定の実体である。**
>
> 実装: `scripts/analyze_seed_variance.py`(新規・読み取り専用・`src/` ゼロタッチ)。
> 既存資産の再利用のみ(`scripts/panel_stats.py` の `_parse_cond_seed` / `t_ci` /
> `perm_test_paired` / `bh_fdr` を **import**。`scripts/analyze_sweep.py` の seed 階層
> ブートストラップと同型)。**`scripts/analyze_g.py` の分解とは別物**である
> (あちらは単一ラン内の**個体**横断分散を g0 と Δg に分けるもので、seed でも条件でもない)。

#### G.1 分解の定義

ラン名の末尾 `_s<数字>` を seed、残りを条件とする(`panel_stats._parse_cond_seed` と同一規則)。
各主要指標 Y について、条件 c ∈ C・seed s ∈ S_c の観測値 y_{c,s} から:

```
mean_c      = (1/|S_c|) Σ_s y_{c,s}                     # 条件 c の seed 平均
V_condition = Var_c( mean_c )            (ddof=1)       # 条件平均の分散 = 決定論的分散 V_d
V_seed      = mean_c[ Var_s( y_{c,s} ) ] (ddof=1)       # 条件内 seed 分散の平均 = 確率的分散 E_s
V_total     = V_condition + V_seed
ratio       = V_condition / V_seed                       ← ★判定に使う量
share_condition = V_condition / V_total,  share_seed = V_seed / V_total
```

- 根拠: **Carmona-Cabrero, Muñoz-Carpena, Oh & Muneepeerakul (2024). *Decomposing Variance
  Decomposition for Stochastic Models.* JASSS 27(1):16. DOI 10.18564/jasss.5174** の
  "Approach IV"。同論文は **乱数 seed をモデル入力として扱ってはならない**とし、総分散を
  決定論的分散 \(V_d\) と確率的分散 \(E_s\) に分けて**両者の寄与率を報告する**ことを推奨する。
  上式の `V_condition` が \(V_d\)、`V_seed` が \(E_s\) に対応する。
- 反復数の決め方: **Lorscheid, Heine & Meyer (2012)** に従い、出力の変動係数(CV)が
  安定する点を目安とする。ラン計画で seed 本数を決める根拠にこれを用いる。

#### G.2 判定規則(**ラン前に固定する。ラン後に変えない**)

| # | 条件 | 閾値 | 意味 |
|---|---|---|---|
| G1 | **ratio > 1.0** | 1.0 | 「条件間差が seed 間差を上回る」の**最小定義**。前文 ④(a) の反証条件そのもの |
| G2 | ratio の **seed 階層ブートストラップ 95%CI 下限 > 1.0** | B = 2000・`default_rng(0)` | seed を条件内で復元抽出して ratio を再計算。**G1 だけでは seed が少ないときに偶然で超える**ため、CI 下限を要求する |
| G3 | 条件内の seed 本数 **|S_c| ≥ 3**(全条件で) | 3 | 2 本では条件内分散の推定が 1 自由度しかなく、ratio が発散しうる |

- **主張してよいのは G1 ∧ G2 ∧ G3 を満たした指標に限る**(前文 ① の「主張は**条件間差が
  seed 間差を上回った指標に限る**(S-05)」の実体)。
- G3 を満たさない指標は **`INSUFFICIENT_SEEDS`** と report し、**有意でも主張しない**
  (§3-E の E4 と同じ作法)。
- `V_seed = 0`(条件内で seed 差がゼロ = 決定論退化)の場合は ratio を `inf` とせず
  **`DEGENERATE_ZERO_SEED_VARIANCE`** と report する。mock ランや LLM キャッシュ完全命中で
  起こりうる状態であり、これを「条件差が圧倒的」と読んではならない。

#### G.3 報告形式

- 条件間差は **d_j = 中央値 [min, max]**(S-16 FormatSpread と同形式)で書く。単一値で書かない。
- **必ず併記する**: `V_condition` / `V_seed` / `ratio` / `share_condition` / `share_seed` /
  ブートストラップ CI / 条件数 / 条件別 seed 本数 / 判定ラベル。
- **この規則で「出さないことにした結論」を報告書 §7.1 に列挙する**(出さなかったことも報告の一部)。

### H. アウトライアを厚くしない設計の宣言(S-08・2026-08-06 追記=**承認対象**)

> 前文 ③(e) に 1 行で書いた設計選択の、**検査可能な形での宣言**。受け皿は
> `docs/plans/observation-report-template.md` §7.2。

1. **設計の主張**: 分野標準(影響力の大きい個体を厚くモデル化し、周辺個体を軽量化・簡略化する)
   と**逆に、本研究は全個体へ一様な認知予算を与える**。理由は研究課題が **k\*(世界改変志向の
   個体)の内生的出現**であり、**「誰が厚くなるか」を設計者が事前に決めた時点で問い自体が
   消える**からである。
2. **代償の明示**: 一様予算は**大規模時の計算効率を捨てている**。この代償は隠さない。
3. **例外の扱い(第 88 バッチ `model.mind`)**: 高解像度層(全体の 1〜5%)は
   **traits に依存しない一様抽選**で割り当てる。**特性が高い個体を優先的に厚くしない**という
   規約であり、本節の設計主張と整合する(`IMPLEMENTED.md` に「S-08 整合」として記録済み)。
4. **片側検査**: `ablate.cognitive_tier`(下位ティア強制)を**片側検査**として用いる。
   - **読み方(必ずこの向きで読む)**: 「一様性が結論を作っていないこと」を**示せる方向にのみ
     効く**。すなわち **ティアを落としても結論が変わらなければ「一様予算が結論を作っていない」
     と言える**が、**変わった場合に「だから厚くすべきだ」とは言えない**(厚さと結論の因果は
     この検査では同定できない)。この非対称性は S-03 の CV 片側バンドと同型である。
   - fleet 非使用ランでは本 ablate は縮退する(効かない)。**縮退したことを縮退したと書く**。
5. **やらないこと**: 影響力の大きい個体を厚くする実装を**本選前に入れない**。入れると
   k\* の内生性の主張が「設計で作った」と読まれる。

### I. 組織形態の観測指標の事前固定(S-09・2026-08-06 追記=**承認対象**)

> **この節が解く問題**: 「どの中心性を使うか」を**後から選ぶのは査読で最も突かれる**。
> Freeman 1979 自身が degree / closeness / betweenness / eigenvector を**別々の理論的主張**として
> 分けており、どれを採るかで「中心的」の意味が変わる。**採る指標と採らない指標を、その理由ごと
> ラン前に固定する。** 実装: `scripts/analyze_org_form.py`(新規・読み取り専用)。

| 軸(サーベイ §4.1.2–4.1.3) | 採る指標 | 一次文献 |
|---|---|---|
| **structure: centralized ↔ decentralized** | **Freeman の群レベル次数中心化指数 \(C_D\)**(スター型 = 1・完全グラフ = 0)。**`degree_gini` と併記**する(Gini は分布の不平等、\(C_D\) はスター型からの距離であって**別の量**である) | Freeman, L. C. (1979). *Centrality in Social Networks.* Social Networks 1:215–239 |
| **structure: layered(層の有無)** | **Global Reaching Centrality (GRC)**(フラット網 ≈ 0・木に近いほど 1)。**有向グラフ**に当てる | Mones, E., Vicsek, L. & Vicsek, T. (2012). PLOS ONE 7(3):e33799 |
| **role: communicator / worker / director** | **Guimerà–Amaral の (z, P) 役割カルトグラフィ**(z = コミュニティ内次数の z スコア / P = 参加係数)。読み替え: 高 P = communicator(橋)/ 高 z = director(内ハブ)/ 低 z 低 P = worker | Guimerà, R. & Amaral, L. A. N. (2005). Nature 433:895–900 |
| **mode: static ↔ dynamic** | **窓間メンバーシップの Jaccard 安定度 + Palla のライフサイクル語彙**(既に `scripts/analyze_communities.py` が実装済み) | Palla, G., Barabási, A.-L. & Vicsek, T. (2007). Nature 446:664–667 |
| 仲介 | **betweenness**(既存の純 Python 実装 `analyze_founders.betweenness_bfs` を再利用。再実装しない) | Freeman 1979 |

**★採らないもの(採らない理由を書くのが S-04 の境界宣言の作法である)**:
- **eigenvector 中心性は採らない**。理由 3 点 — (a) repo 内に実装が無く、新規に書く唯一の中心性に
  なる、(b) `nx.eigenvector_centrality` は冪乗法で**収束しないグラフがある**(決定論性の主張と
  相性が悪い)、(c) 「誰が影響力を持つか」は S-11(**本選後**)の主題であって、S-09(組織の**形**)の
  主題ではない。
- **Gini の 6 個目・中心性の 3 個目の実装を書かない**(既存実装を import する)。

**★Guimerà–Amaral の閾値について(事前の宣言)**: 原典の閾値 **z = 2.5 / P = 0.62 は代謝
ネットワークで較正された値**であり、社会ネットワークにそのまま使える保証は無い。したがって
**既定は閾値を使わず、(z, P) 平面上の分布そのもの(分位点)を報告する**。閾値による
4 分類の件数は **参考値**としてのみ併記し、**主判定には用いない**。

---

## 4. 判定式(合格/不合格 → D1 アフォーダンス ON 判断への接続)

```
primary  = (p(2,5) < 0.05) ∧ (TVD(2,5) ≥ 0.05) ∧ (R(2,5) ≥ 1.50)
sustained = (p(late) < 0.05) ∧ (TVD(late) ≥ 0.05) ∧ (R(late) ≥ 1.50)      # late = late_same_lag ペア

ラベル:
  primary ∧ sustained          → SUSTAINED_NONSTATIONARY   (持続的に非定常)
  primary ∧ ¬sustained         → TRANSIENT_ONLY            (非定常性は立ち上がりの過渡のみ)
  ¬primary                     → STATIONARY_LIKE           (Day t と Day t' が区別できない = 反復)
  primary ∧ (late ペアが取れない) → NONSTATIONARY_UNVERIFIED (過渡と分離できない)
  判定に必要な量が欠測          → INCONCLUSIVE
```

この判定式は `scripts/diagnose_stationarity.py` の `_verdict()` に**逐語で実装済み**
(`verdict.label` / `verdict.primary_pass` / `verdict.sustained_pass`)。文書と実装が食い違ったら
実装ではなく本文書が正典であり、実装を直す。

### D1(場所の意味づけ最小版・命名の場所束縛)の ON/OFF への接続

| ラベル | 本選 10 日ランでの D1 | 理由 |
|---|---|---|
| SUSTAINED_NONSTATIONARY | **OFF のまま** | 日常が反復に閉じていない = 構造的手当ての必要が示されていない。R1 ドクトリン(本線を汚さない)を優先 |
| TRANSIENT_ONLY | 本線は **OFF のまま**。D1 ON は**別条件ラン**として GPU 余剰時にのみ。**D1 の必要性は「あり」と記録する** | 常態は定常 = 原指示 D の危惧が(過渡の後に)当たっている。ただし 10 日ランはほぼ全部が過渡なので、本選内では D1 の効果を分離できない。本線は現実幾何 1 条件(H_real 基準点)を守る |
| STATIONARY_LIKE | **D1 ON を提案**(ユーザー最終判断)。ただし本線 10 日ランに入れるか別条件にするかは、コード凍結原則(計画書 §2 レーン2)と天秤 | 反復に収束している = 原指示 D の想定が現実化。ただし ON は本線の既定挙動変更なので、ゴールデン・呼数不変の検収を通したうえでユーザー承認が要る |
| NONSTATIONARY_UNVERIFIED | **OFF のまま** + 日数を伸ばして再測定 | 後期ペアが取れない(日数不足)= 過渡と常態を分離できていない。分離できないまま「非定常だから手当て不要」と結論しない |
| INCONCLUSIVE | OFF のまま + 診断の再設計 | 判定不能を「合格」に読み替えない |

> **mock 事前測定の時点で `daily300_100d` = TRANSIENT_ONLY**。この行がそのまま実 LLM でも出れば、
> 「D1 は必要だが本選の 10 日ランでは検証できない」= **本選後(レーン3)へ回す**という結論になる。

**このゲートは自動実行しない。** `scripts/diagnose_stationarity.py` は機械判定を出すだけで、
D1 の ON/OFF は必ずユーザーが決める(記憶則 `ask-before-extending`)。

### 適用スケジュール

1. **(済) mock 事前測定** — `daily300_100d` / `endo7_kfree_eoff_s11`(§2)。配線確認 + 構造系の基準値。
2. **GPU 開放初日(8/15-16)の実 LLM 診断ラン** — 数百体 × 20 sim日相当の短縮版 1 本。
   **これが確定判定に使うラン**。実行前に本文書を承認・凍結する(U-10)。
3. 本選 10 日ラン(8/16〜8/26)後に同じ CLI を再適用し、**本番ランそのものの非定常性**も報告する。

---

## 5. 「反復した」場合の報告条項(設計空間の上界として報告する)

判定が **STATIONARY_LIKE**(区別できない)または **TRANSIENT_ONLY**(立ち上がりの過渡だけで、
常態は定常)になった場合でも、それは失敗ではなく**測定された事実**として報告する。
mock 事前測定では `daily300_100d` が既に TRANSIENT_ONLY(day 18 以降は定常)なので、
実 LLM でも同じ形になる可能性は十分ある。具体的には次の形で書く:

> 「現行のアフォーダンス集合(受理動詞 21 型・世界改変 6 種・既定 OFF の反収束機構は OFF のまま)と
> 現実渋谷の幾何のもとで、N = ○○ 体 × D 日の日常過程は、lag L 日離れた 2 日を
> 日間ノイズ床(F = ○○)を超えて区別できなかった(TVD = ○○, R = ○○, p = ○○)。
> これは **この設計空間が生む多様性の上界**の測定であって、LLM 社会一般の上界ではない。」

**上界としての正しい読み方**(報告時に必ず添える):

- 上界を規定しているのは (i) 決定論日次予定の骨格、(ii) EPR の優先的回帰(正のフィードバック)、
  (iii) 反収束機構(退屈ループ `drive.boredom` / 価値飽和 / 閾値ドリフト `theta_drift` /
  Gumbel 確率的行き先選択)が**全部既定 OFF** であること — の 3 点であり、これらは**変更可能な設計選択**である。
- したがって「反復した」は「LLM エージェント社会は反復する」ではなく
  「**この構成では**反復する。反復を破る設計余地は少なくとも 3 つ特定されている」と述べる。
- 規模(N)を上げても同じ結論になるとは限らない。本測定は N=300 と N=60 の 2 点しかなく、
  ノイズ床 F が N に強く依存する(0.049 vs 0.123)ことは確認済みだが、
  **N を増やしたときに R がどう動くかは未測定**である(正直な限界)。
- 逆に **SUSTAINED_NONSTATIONARY** が出た場合も、それを「創発が起きた」と読み替えない。
  日次プロフィールが区別できることは、蓄積(語彙・関係・訪問履歴)が進行中であることを意味するだけで、
  質的な新規性の証明ではない。

---

## 6. 正直原則(mock 実測の限界)

**mock ランの語彙・発話は決定論である**(`src/society/llm/mock.py` = 決定論 JSON アクション)。したがって:

- **語彙系(§1.1)の水準は実 LLM と挙動が違う。** mock の造語は決定論テンプレートから出るので、
  新語の出方・尽き方が実 LLM の生成分布とは別物である。§2 の ν の絶対値
  (daily300 の後半 0.0019 など)を実 LLM ランの合否基準にそのまま使ってはならない。
  → **C1(ν の閾値)は実 LLM 診断ランで再較正する。**
- **§2 の mock 実測の位置づけは (a) 配線確認(ツールが 11.9M 行を捌けること・指標が動くこと)と
  (b) 構造系指標の基準値** である。構造系(関係グラフ・訪問・自己相似性)は移動と接触の機構が
  生むもので、これらは mock でも実 LLM でも**同じ非 LLM 経路**(routine + EPR + perception)が
  駆動する。よって A/B の閾値(TVD・R)は mock 基準値をそのまま初期値に採ってよいと判断した。
  ただし実 LLM では発話量が変わり `speak`/`hear` の kind 比が動くので、**ノイズ床 F は実 LLM で
  取り直す**(F はランごとに CLI が自動計算するので、閾値を触らずに追随する = R を主軸にした理由の一つ)。
- 実 LLM 診断ランで再較正するのは **C1(ν)のみ**を予定する。A/B(TVD・R)を再較正する場合は、
  それ自体が閾値変更なので §7 に記録し、変更前後の判定を両方報告する。
- `daily300_100d` は 2026-07-09 生成の既存ランであり、その後の第60-64バッチの変更は反映されていない
  (relations の内生化・gossip・quality 等は当時の既定のまま)。**時点の異なるランである**ことを明記する。

---

## 7. 変更履歴(承認後はここに全変更を記録する)

| 日付 | 変更 | 理由 | 変更前後の判定 |
|---|---|---|---|
| 2026-07-29 | 初版作成(ドラフト) | D0 バッチ | — |

**未決事項(承認時に一緒に決める)**

- [ ] 実 LLM 診断ランの規模・日数の確定(計画書は「数百体 × 20 sim日相当の短縮版」。
      burn_in_end ≈ 18 日なので、**常態を測るには 20 日でも足りない可能性**がある — 20 日だと
      late_same_lag が過渡の中に入る。ユーザー判断が要る)。
- [ ] C1(語彙新規性)閾値の実 LLM 再較正。
- [ ] N 依存性(ノイズ床 F と R の N スケーリング)の測定を P0 ベンチに乗せるか。
- [ ] **前文「主張の境界」(S-04)の承認**(2026-08-05 追記)。特に ③(d) の「分散比が閾値を
      下回った指標は主張を集団レベルの定性パターンに限定する」の**閾値そのもの**は未定。
      実 LLM ランの分散表(`calibrate_report.py` の個体間分散節)を見てから固定する。
- [ ] **§3-F stylized facts(S-02)の承認**(2026-08-05 追記)。主判定 4 件(F5/F6/F4/F7)・
      参考 6 件・不採用 2 件の**二分**と、F7 の dunbar OFF 条件限定を承認対象とする。
      F1 の θ(谷/ピーク比)を数値で置くか、定性判定に留めるかもここで決める。
